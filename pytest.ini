[pytest]
testpaths =
    trader/tests
    extensions/Backtesting/tests
markers =
    slow: slower integration-style checks that are useful before handoff but not needed for every small edit
    backtest_engine: mocked BacktestEngine replay checks
    runtime_kernel: StrategyRuntime kernel and registry checks
    plugin_unit: focused StrategyPlugin unit checks
